---
phase: quick-260329-n58
plan: 1
status: complete
date_completed: 2026-03-29T16:40:56Z
duration_minutes: 3
completed_by: Claude Haiku 4.5
---

# Quick Task 260329-n58: Fix ScoringAgentService Test Constructor Error

## Summary

Fixed ScoringAgentService test instantiation by providing both required dependencies (ConfigService + JobTitleMatcherService) instead of just ConfigService. The service constructor was updated in Phase 6.5 to support semantic job title matching but the test was not updated to match the new signature.

## What Was Done

**Task 1: Fix ScoringAgentService test makeService() to provide both dependencies**

Updated `src/scoring/scoring.service.spec.ts`:
- Added `JobTitleMatcherService` import (line 2)
- Modified `makeService()` factory function to create mock for `JobTitleMatcherService` with `matchJobTitles()` method returning `{ matched: boolean, confidence: number }` (lines 20-21)
- Passed both `configService` and `jobTitleMatcher` to `ScoringAgentService` constructor (line 22)
- Fixed inline test at line 65 that directly instantiated service to also provide both dependencies

## Verification

Test run output:
```
Test Suites: 1 passed, 1 total
Tests:       7 passed, 7 total
```

All 7 tests pass without errors:
- SCOR-03: score() calls callModel with openai/gpt-4o-mini model
- reads OPENROUTER_API_KEY from ConfigService
- SCOR-05: score() returns modelUsed = "openai/gpt-4o-mini"
- SCOR-03 shape: result passes ScoreSchema validation
- throws when callModel().getText() rejects
- throws when LLM output fails ScoreSchema validation
- strips markdown code fences from model response

## Files Modified

- `src/scoring/scoring.service.spec.ts` (+9 lines, -2 lines)

## Commits

| Hash | Message |
|------|---------|
| a655ab3 | fix(quick-260329-n58): update ScoringAgentService test to mock both ConfigService and JobTitleMatcherService |

## Deviations from Plan

None — plan executed exactly as written. Test implementation matched the provided pattern from extraction-agent.service.spec.ts and successfully mocks all dependencies as required.
