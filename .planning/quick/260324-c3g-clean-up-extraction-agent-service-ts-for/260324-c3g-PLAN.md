---
phase: quick-260324-c3g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ingestion/services/extraction-agent.service.ts
  - prisma/seed.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "extraction-agent.service.ts always attempts AI extraction — no flag gate"
    - "Swapping AI provider is isolated to a single private method or client init block"
    - "Seed produces two candidate records in the Triolla tenant after npx prisma db seed"
  artifacts:
    - path: "src/ingestion/services/extraction-agent.service.ts"
      provides: "AI extraction without ENABLE_AI_EXTRACTION flag"
    - path: "prisma/seed.ts"
      provides: "Two seeded candidates for Triolla tenant"
  key_links:
    - from: "extraction-agent.service.ts"
      to: "@openrouter/sdk"
      via: "private callOpenRouter() method — all provider-specific code isolated here"
---

<objective>
Remove the `ENABLE_AI_EXTRACTION = false` feature flag from extraction-agent.service.ts so AI is
always attempted, and isolate all provider-specific code so swapping back to `@ai-sdk/anthropic`
requires changing only one private method. Also add two seed candidates to prisma/seed.ts for
testing the upcoming job creation feature.

Purpose: The flag was a temporary scaffold that's now dead code hiding the real execution path
from tests. Removing it makes the spec's existing OpenRouter mocks actually exercise the live
code path. Isolating provider code makes the future `@ai-sdk/anthropic` swap a 10-line change.

Output: Cleaned extraction service (no flag, provider isolated) + two candidate seed records.
</objective>

<execution_context>
@/Users/danielshalem/triolla/telent-os-backend/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielshalem/triolla/telent-os-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove ENABLE_AI_EXTRACTION flag and isolate provider code</name>
  <files>src/ingestion/services/extraction-agent.service.ts</files>
  <action>
Rewrite the service to remove the `ENABLE_AI_EXTRACTION = false` const and its if-branch entirely.
AI extraction is now always attempted; deterministic extraction is fallback-only.

Isolate all `@openrouter/sdk`-specific code into a single private method `callAI(fullText: string)`
that returns the raw parsed `CandidateExtract` fields (without `suspicious`). This is the only
method that touches the OpenRouter client or model name. When swapping to `@ai-sdk/anthropic`
later, only this method changes.

The public `extract(fullText, suspicious)` method should:
1. Call `this.callAI(fullText)` in a try/catch
2. On success: merge with `{ suspicious }` and return
3. On error: log the error, call `this.extractDeterministically(fullText)`, merge with `{ suspicious }`, and return

`callAI` should contain:
- OpenRouter client init (reads `OPENROUTER_API_KEY` from configService)
- `client.callModel(...)` call with current model and INSTRUCTIONS
- `getText()` call
- Markdown code fence stripping
- `CandidateExtractSchema.parse(JSON.parse(json))` call
- Return the parsed object

Keep `FALLBACK`, `INSTRUCTIONS`, `CandidateExtractSchema`, `CandidateExtract` type, and
`extractDeterministically` unchanged.

Add a comment above `callAI`:
```
// AI_PROVIDER: swap this method to change provider (e.g. @ai-sdk/anthropic generateObject)
// Current: @openrouter/sdk — google/gemini-2.0-flash:free
```

Do NOT change the public method signature or exports. The spec file mocks `@openrouter/sdk` and
will now exercise the real code path on every test — verify all existing tests pass.
  </action>
  <verify>
    <automated>npx jest --testPathPattern=extraction-agent.service.spec --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>
All 8 tests in extraction-agent.service.spec.ts pass. No `ENABLE_AI_EXTRACTION` const in the file.
`callAI` private method exists and is the only place `@openrouter/sdk` is referenced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add two seed candidates to prisma/seed.ts</name>
  <files>prisma/seed.ts</files>
  <action>
After the existing tenant upsert, add upsert calls for two candidates in the Triolla tenant
(id: '00000000-0000-0000-0000-000000000001').

Use `prisma.candidate.upsert` with `where: { id: ... }` and hardcoded UUIDs so the seed is
idempotent. Do NOT create Application records — there is no seeded Job yet.

Candidate 1 — Senior software engineer:
```
id: '00000000-0000-0000-0000-000000000101'
tenantId: '00000000-0000-0000-0000-000000000001'
fullName: 'Yael Cohen'
email: 'yael.cohen@example.com'
phone: '+972-50-111-2222'
currentRole: 'Senior Software Engineer'
location: 'Tel Aviv, Israel'
yearsExperience: 7
skills: ['TypeScript', 'Node.js', 'React', 'PostgreSQL', 'Docker']
source: 'email'
aiSummary: 'Senior fullstack engineer with 7 years experience. Strong TypeScript and React skills with production PostgreSQL experience.'
```

Candidate 2 — Junior product manager:
```
id: '00000000-0000-0000-0000-000000000102'
tenantId: '00000000-0000-0000-0000-000000000001'
fullName: 'Noam Levy'
email: 'noam.levy@example.com'
phone: '+972-52-333-4444'
currentRole: 'Product Manager'
location: 'Haifa, Israel'
yearsExperience: 2
skills: ['Product Strategy', 'Roadmapping', 'SQL', 'Figma']
source: 'email'
aiSummary: 'Junior PM with 2 years in B2B SaaS. Experience with roadmap planning and cross-functional stakeholder management.'
```

Log each upserted candidate id with `console.log('Upserted candidate:', candidate.id)`.
  </action>
  <verify>
    <automated>npx ts-node prisma/seed.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
Seed runs without error and logs three lines: tenant id + two candidate ids.
Re-running is idempotent (upsert — no unique constraint errors on second run).
  </done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions from the service refactor:
`npx jest --no-coverage 2>&1 | tail -5`
Expected: all suites pass, total test count unchanged or higher.
</verification>

<success_criteria>
- `ENABLE_AI_EXTRACTION` const is gone from extraction-agent.service.ts
- Private `callAI()` method contains all `@openrouter/sdk` code; comment marks it as the swap point
- All 8 extraction service tests pass with mocked OpenRouter (they now test the real code path)
- `prisma/seed.ts` produces two seeded candidates idempotently
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/quick/260324-c3g-clean-up-extraction-agent-service-ts-for/260324-c3g-SUMMARY.md`
</output>
