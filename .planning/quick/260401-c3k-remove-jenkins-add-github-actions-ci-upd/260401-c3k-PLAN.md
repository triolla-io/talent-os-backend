---
task: 260401-c3k
type: quick
autonomous: true
files_modified:
  - Jenkinsfile
  - docker-compose.yml
  - .github/workflows/ci.yml

must_haves:
  truths:
    - 'No Jenkins files exist in the repo'
    - 'GitHub Actions CI runs build + test on push/PR to main'
    - 'docker-compose.yml contains only api, worker, postgres, redis — no nginx or certbot'
  artifacts:
    - path: '.github/workflows/ci.yml'
      provides: 'GitHub Actions CI pipeline (build + test)'
    - path: 'docker-compose.yml'
      provides: 'App-only compose (no nginx, no certbot, no letsencrypt volumes)'
  key_links:
    - from: '.github/workflows/ci.yml'
      to: 'npm run build && npm test'
      via: 'GitHub Actions steps'
---

<objective>
Remove Jenkins CI entirely and replace it with GitHub Actions. Clean up docker-compose.yml so it only contains app services — Coolify will manage TLS, reverse proxy, and deployment.

Purpose: Jenkins is being replaced by Coolify (self-hosted PaaS on Hetzner). GitHub Actions handles the CI gate (build + test). Coolify handles deployment and TLS — so nginx and certbot are no longer needed in docker-compose.yml.

Output: Jenkinsfile deleted, .github/workflows/ci.yml created, docker-compose.yml trimmed to 4 services.
</objective>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Jenkins, create GitHub Actions CI workflow</name>
  <files>Jenkinsfile, .github/workflows/ci.yml</files>
  <action>
    1. Delete `/Users/danielshalem/triolla/telent-os-backend/Jenkinsfile` using the Bash tool: `rm /Users/danielshalem/triolla/telent-os-backend/Jenkinsfile`

    2. Create `.github/workflows/ci.yml` with the following content:
       - Trigger: `push` and `pull_request` targeting `main`
       - Single job: `ci` running on `ubuntu-latest`
       - Steps:
         a. `actions/checkout@v4`
         b. `actions/setup-node@v4` with `node-version: '20'` and `cache: 'npm'`
         c. `npm ci`
         d. `npm run build`
         e. `npm test` with env var `NODE_ENV=test`
       - No deploy step — Coolify handles deployment.
       - No Docker build step — not needed for CI gate.

  </action>
  <verify>
    <automated>ls /Users/danielshalem/triolla/telent-os-backend/Jenkinsfile 2>&1 | grep -q "No such file" && echo "Jenkinsfile deleted OK" && cat /Users/danielshalem/triolla/telent-os-backend/.github/workflows/ci.yml</automated>
  </verify>
  <done>Jenkinsfile is gone. .github/workflows/ci.yml exists with checkout, setup-node, npm ci, npm run build, npm test steps. No deploy step present.</done>
</task>

<task type="auto">
  <name>Task 2: Remove nginx and certbot from docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>
    Rewrite docker-compose.yml keeping only these 4 services: api, worker, postgres, redis.

    Remove entirely:
    - The `nginx` service (image: nginx:alpine, ports 80/443, volumes for nginx.conf and letsencrypt)
    - The `certbot` service (image: certbot/certbot, renewal entrypoint)
    - The `letsencrypt_data` volume
    - The `certbot_webroot` volume

    For the `api` service, add back the port mapping that was removed when nginx was introduced:
    - `ports: - '3000:3000'`
    This allows Coolify's reverse proxy to reach the api container directly.

    Keep all other service config unchanged: env_file, environment, depends_on, healthcheck, restart, deploy resource limits.

    The final `volumes:` block should only contain `postgres_data:` and `redis_data:`.

  </action>
  <verify>
    <automated>cd /Users/danielshalem/triolla/telent-os-backend && grep -c "nginx\|certbot\|letsencrypt" docker-compose.yml && echo "Expected: 0 matches" || true; docker compose config --quiet 2>&1 | head -5</automated>
  </verify>
  <done>docker-compose.yml has no nginx, certbot, or letsencrypt references. `docker compose config` validates cleanly. api service exposes port 3000. Only postgres_data and redis_data volumes remain.</done>
</task>

</tasks>

<verification>
- `Jenkinsfile` does not exist in repo root
- `.github/workflows/ci.yml` exists and contains `npm run build` and `npm test` steps, no deploy step
- `docker-compose.yml` services: api, worker, postgres, redis — nothing else
- `docker compose config` runs without errors
</verification>

<success_criteria>
Jenkins fully removed. GitHub Actions CI workflow in place (build + test gate). docker-compose.yml is app-only — Coolify owns TLS and reverse proxy from this point forward.
</success_criteria>

<output>
After completion, update `.planning/STATE.md` Quick Tasks table with this entry:
| 260401-c3k | Remove Jenkins, add GitHub Actions CI, strip nginx/certbot from docker-compose | 2026-04-01 | {commit} | [260401-c3k-remove-jenkins-add-github-actions-ci-upd](./quick/260401-c3k-remove-jenkins-add-github-actions-ci-upd/) |
</output>
