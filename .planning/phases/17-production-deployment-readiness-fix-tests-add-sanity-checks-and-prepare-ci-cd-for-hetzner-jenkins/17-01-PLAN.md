---
phase: 17-production-deployment-readiness-fix-tests-add-sanity-checks-and-prepare-ci-cd-for-hetzner-jenkins
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/jobs/jobs.integration.spec.ts
  - src/ingestion/ingestion.processor.spec.ts
  - src/ingestion/ingestion.processor.ts
autonomous: true
requirements:
  - D-10
  - D-12
  - D-13

must_haves:
  truths:
    - 'npm run test exits 0 with 0 failures'
    - 'jobs.integration.spec.ts: all 4 PUT /jobs/:id tests pass'
    - 'ingestion.processor.spec.ts: all 6 Phase 15 job routing tests pass'
  artifacts:
    - path: 'src/jobs/jobs.integration.spec.ts'
      provides: 'Fixed mock tx with candidate.updateMany mock'
      contains: 'candidate: { updateMany: jest.fn()'
    - path: 'src/ingestion/ingestion.processor.ts'
      provides: "Uses status: 'open' consistently (or tests updated to match 'open')"
    - path: 'src/ingestion/ingestion.processor.spec.ts'
      provides: "Tests use status: 'open' to match processor reality"
  key_links:
    - from: 'jobs.integration.spec.ts makePutPrisma()'
      to: 'jobs.service.ts tx.candidate.updateMany()'
      via: 'mockTx must expose candidate object'
      pattern: 'candidate.*updateMany'
    - from: "ingestion.processor.spec.ts status: 'active'"
      to: "ingestion.processor.ts status: 'open'"
      via: 'test expectation must match production code'
      pattern: "status.*'open'"
---

<objective>
Fix all 6 failing unit tests caused by Phase 16 changes. Tests currently fail because:
1. `jobs.integration.spec.ts` — `makePutPrisma()` mock transaction lacks a `candidate` object; `jobs.service.ts` calls `tx.candidate.updateMany()` (added in Phase 16) but the mock only has `{ job: { update } }`.
2. `ingestion.processor.spec.ts` — 2 tests expect `status: 'active'` but `ingestion.processor.ts` queries with `status: 'open'`. The processor is correct (job status is 'open'); the tests have a stale expectation.

Purpose: CI gate requires 0 failing tests. These failures block Jenkins pipeline validation and every subsequent task verification.
Output: `npm run test` exits 0 with 290 tests passing, 0 failures.
</objective>

<execution_context>
@/Users/danielshalem/triolla/telent-os-backend/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielshalem/triolla/telent-os-backend/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-production-deployment-readiness-fix-tests-add-sanity-checks-and-prepare-ci-cd-for-hetzner-jenkins/17-CONTEXT.md

<interfaces>
<!-- Key facts about the failing tests. Executor MUST read the actual files before editing. -->

Failure 1 — jobs.integration.spec.ts (3 failures in PUT /jobs/:id block):
Root cause: makePutPrisma() creates mockTx = { job: { update: jest.fn() } }
jobs.service.ts updateJob() calls tx.candidate.updateMany() (line 167) and tx.application.updateMany() (line 173)
mockTx does not expose .candidate or .application → TypeError: Cannot read properties of undefined
Fix: Add candidate and application objects to mockTx inside makePutPrisma():
const mockTx = {
job: { update: jest.fn().mockResolvedValue(mockJob) },
candidate: { updateMany: jest.fn().mockResolvedValue({ count: 0 }) },
application: { updateMany: jest.fn().mockResolvedValue({ count: 0 }) },
};

Failure 2 — ingestion.processor.spec.ts (2 failures in Phase 15 section):
Test at line 638: expect(prisma.job.findMany).toHaveBeenCalledWith(
expect.objectContaining({ where: { tenantId: 'test-tenant-id', status: 'active' } })
)
Processor at src/ingestion/ingestion.processor.ts uses: where: { tenantId, status: 'open' }
The processor is correct (job status enum is 'open'/'closed'/'draft', NOT 'active').
Fix: Update the 2 test expectations to use status: 'open' instead of status: 'active'.
Lines to update in ingestion.processor.spec.ts: ~638 and ~778.

Failure 3 — jobs.integration.spec.ts (1 failure in GET /jobs):
Test: 'candidate_count reflects applications count'
Root cause: prisma mock for findMany does not have \_count.candidates property
The mock job in makeMockJob() may need \_count: { candidates: N } to match what the service returns.
Examine the existing makeMockJob() helper and the GET /jobs test setup before fixing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PUT /jobs/:id mock transaction — add candidate and application to mockTx</name>
  <read_first>
    - src/jobs/jobs.integration.spec.ts (read the full makePutPrisma() function and surrounding test block, lines 383–450)
    - src/jobs/jobs.service.ts (read the updateJob() method to see all tx.* calls made inside the transaction)
  </read_first>
  <files>src/jobs/jobs.integration.spec.ts</files>
  <action>
In `src/jobs/jobs.integration.spec.ts`, find the `makePutPrisma()` helper function (around line 386). The current `mockTx` only has `{ job: { update: jest.fn().mockResolvedValue(mockJob) } }`.

Replace the mockTx definition inside `makePutPrisma()` with:

```typescript
const mockTx = {
  job: { update: jest.fn().mockResolvedValue(mockJob) },
  candidate: { updateMany: jest.fn().mockResolvedValue({ count: 0 }) },
  application: { updateMany: jest.fn().mockResolvedValue({ count: 0 }) },
};
```

Also read the GET /jobs describe block to find the 'candidate_count reflects applications count' failing test. Identify what `_count.candidates` the mock job returns vs what the test expects. Fix the mock or expectation so they align.

Do NOT change any test logic beyond the mockTx fix and the candidate_count alignment. Do NOT add new tests.
</action>
<verify>
<automated>npm run test -- src/jobs/jobs.integration.spec.ts 2>&1 | tail -5</automated>
</verify>
<acceptance_criteria> - Command `npm run test -- src/jobs/jobs.integration.spec.ts` exits 0 - Output contains "Tests: 39 passed" (or higher if count changed) and "0 failed" - `jobs.integration.spec.ts` contains `candidate: { updateMany: jest.fn()` inside makePutPrisma
</acceptance_criteria>
<done>All jobs.integration.spec.ts tests pass with 0 failures.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ingestion.processor.spec.ts — update stale 'active' status expectations to 'open'</name>
  <read_first>
    - src/ingestion/ingestion.processor.spec.ts (read lines 620–800 to find all status: 'active' expectations)
    - src/ingestion/ingestion.processor.ts (read extractAllJobIdsFromEmailText() to confirm it uses status: 'open')
  </read_first>
  <files>src/ingestion/ingestion.processor.spec.ts</files>
  <action>
In `src/ingestion/ingestion.processor.spec.ts`, find all test expectations that use `status: 'active'` in a `where` clause asserting against `prisma.job.findMany`. There are exactly 2 such occurrences (around lines 638 and 778).

For each, change:

```typescript
where: { tenantId: 'test-tenant-id', status: 'active' }
```

to:

```typescript
where: { tenantId: 'test-tenant-id', status: 'open' }
```

The processor is correct — job status in this codebase is 'open'/'closed'/'draft' (as per the Prisma schema and CreateJobSchema which uses `z.enum(['draft', 'open', 'closed'])`). The tests had a stale 'active' expectation from before the schema was finalized.

Do NOT change any other test logic. Do NOT change the processor source file.
</action>
<verify>
<automated>npm run test -- src/ingestion/ingestion.processor.spec.ts 2>&1 | tail -5</automated>
</verify>
<acceptance_criteria> - Command `npm run test -- src/ingestion/ingestion.processor.spec.ts` exits 0 - Output contains "0 failed" - File no longer contains the string `status: 'active'` in any `where` expectation block - File still contains `status: 'open'` in the corrected expectations
</acceptance_criteria>
<done>All ingestion.processor.spec.ts tests pass with 0 failures.</done>
</task>

<task type="auto">
  <name>Task 3: Verify full test suite is green</name>
  <read_first>
    - (none — just run the suite)
  </read_first>
  <files></files>
  <action>
Run `npm run test` to confirm the full suite passes. If any additional failures are found beyond the 6 targeted above, fix them. The target state is 0 failures.

If there are additional failures unrelated to the 6 known ones, investigate and fix them. Document any fix in the task commit message.
</action>
<verify>
<automated>npm run test 2>&1 | tail -5</automated>
</verify>
<acceptance_criteria> - `npm run test` exits 0 - Output line contains "Tests: X passed, 0 failed" (X >= 290) - Output line contains "Test Suites: Y passed, 0 failed" (Y >= 21)
</acceptance_criteria>
<done>Full unit test suite passes with 0 failures.</done>
</task>

</tasks>

<verification>
Full test suite verification:
```bash
npm run test 2>&1 | tail -8
```
Expected: "Test Suites: 21 passed, 0 failed" and "Tests: X passed, 0 failed"
</verification>

<success_criteria>

- `npm run test` exits 0 with 0 failing tests
- jobs.integration.spec.ts: all 4 PUT /jobs/:id tests pass (mockTx has candidate + application)
- ingestion.processor.spec.ts: all Phase 15 routing tests pass (status 'open' not 'active')
- No new test failures introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/17-production-deployment-readiness-fix-tests-add-sanity-checks-and-prepare-ci-cd-for-hetzner-jenkins/17-01-SUMMARY.md`
</output>
